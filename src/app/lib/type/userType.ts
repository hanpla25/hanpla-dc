export type UserPayload = {
  user_id: string;
  user_name: string;
  created_at: string;
};

export type UserData = {
  name: string;
  write_count: number;
  comment_count: number;
  created_at: string;
};
